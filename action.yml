# action.yml
name: "huacnlee.autocorrect"
description: "Use autocorrect to check."
inputs:
  args: # id of input
    description: "args for autocorrect"
    required: false
    default: "./ --lint"
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.args }}
